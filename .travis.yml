language: c
before_install:
 - sudo apt-get update -qq
 - sudo apt-get install -qq librsync-dev libconfuse-dev libfuse-dev
compiler:
  - gcc
script: ./configure --disable-maintainer-mode && make && make check
after_script: cat testsuite.log
